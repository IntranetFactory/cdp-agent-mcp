#!/usr/bin/with-contenv bash

# Wait for Chrome CDP endpoint to be ready
for i in $(seq 1 30); do
  curl -s http://127.0.0.1:9222/json/version > /dev/null 2>&1 && break
  sleep 1
done

exec node /app/build/src/index.js --config /config/cdp-agent-mcp.config.json
